const toggle =  ducument.getElementby('toggle');
const close =  ducument.getElementby('close');
const open =  ducument.getElementby('open');
const modal = document.getElementById('modal');

// add Event listenerto toggle button

toggle.addEeventListener("click", ()=> {
modal.classList.add('show-modal');
})

// add event listener to add button
open.classList
